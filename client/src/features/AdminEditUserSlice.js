import { createSlice } from "@reduxjs/toolkit";


// this is a non asynchronous global state management.
const initialState = {
  loading: false,
  UserToEdit: null,
  error: null,
}

const editUser=


const AdminUserEditSlice = createSlice({
  name: "AdminEditUser",
  initialState,
  reducer: {

  }
})